# Web components + drag and drop proof of concept

A simple demo web app showing how to implement drag&drop feature in vanilla HTML + vanilla JS + vanilla CSS.

## Installation

```
npm install
npm start
```